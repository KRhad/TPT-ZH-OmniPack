# 更新日志

> 本项目使用 OpenAI Codex / AI 辅助进行审计、实现、文档与自动测试；测试边界见 `docs/AI_DISCLOSURE.md`。

## 0.4.0（开发中）

- 新增默认关闭的独立炼金探索模式；普通沙盒保持自由，进度按 OPS 存档隔离。
- 新增 schema 1 的四初始元素、十阶段解锁图，实际覆盖温度、压力、电流、催化、时间、结构、冷却、过滤和多阶段过程。
- 搜索、收藏、活动工具、粘贴、普通 Lua 创建/改型/属性/工具和 stamp 使用统一锁定门禁；锁定与自由模式实际回归通过。
- 进度对象使用稳定 identifier 和严格键/类型/数量/长度限制；损坏或未知 schema fail-closed，不改写来源存档。
- 模块开启和关闭各完成十阶段真实运行；精通状态连续 100 次 `Serialise -> Parse -> Import` 无丢失、重复或死局。
- 新增显式 `0.4.0-dev/local-dev` 普通包与 Symbols 包，继承 0.2/0.3 内容并封入版本化炼金图；固定构建时间后 EXE 与两次 ZIP 封包可复现，解压二审通过。
- 本地门禁为 clean build `510/510`、Meson `18/18`、Python `133/133`、0 skip。炼金窗口、完整双语/DPI、公开源码/tag/Release 仍未完成，因此 `release_ready=false`。

## 0.3.0（开发中）

- 登记 11 类工程自动化能力，全部复用官方电子元件，没有新增自动化元素，稳定 ID `392..423` 保持预留。
- 新增 9 个真实 OPS 自动化场景与 6 项挑战；实际运行 `9/9`、`6/6`、95 条断言且停止后事件增量为 0。
- 48 个模组元素说明统一先显示对应英文/中文名称，例如 `Aluminium: ...` 与 `铝：……`。
- S11/S12 正式压力样本通过；一次性火花失败样本保留，改用官方 `BTRY` 持续激励后重新完成全时长验证。
- `0.3.0-dev` 普通包与 Symbols 包二审通过；可信 GUI 与公开发布门禁仍未完成。

## 0.2.0（开发中）

- 新增 48/48 OmniPack 元素用途矩阵，逐项记录生产、主要/次要用途、消耗、副产物、危险、控制、回收、教程、压力风险、缺口与稳定 ID 处置。
- 新增 fail-closed 用途审计，拒绝缺行、额外元素、稳定 ID/代号/模块漂移、非法缺口分类以及“有缺口却标记保持不变”。
- 登记并实现四条最小跨模块链：`PERO + PATH` 病原处理、湿 `HUMS + FERT` 营养回收、`SLAG + ACID + CATA` 助熔剂回收、`SSIL + 熔融 LEAD + SPRK(NCRM)` 屏蔽组装，以及冷却四模块废物稳定化；均固定 `3x3`、复用模块预算并保持稳定 ID 不变。
- 新增 7 个版本化 OPS 示例和 8 项双语教程/挑战；候选 EXE 已实际生成、加载并完成 `8/8` 教程断言，清单固定稳定加载 ID、粒子数、字节数和 SHA-256。
- 新增显式 `0.2.0-dev/local-dev` 本地包配置，普通包封入白名单教程与示例，且不覆盖或冒充 `0.1.0-test/public-test`；包、符号、PE、成员哈希和解压启动二审通过。
- 最终门禁为 Meson `15/15`、Python `109/109`（0 skip）、Lua 6 次实际运行、官方/四模块/混合 OPS `6/6`；S04/S05/S07/S09 各完成 `60+600` 秒并独立判定 `4/4 gate=true`。可信 GUI、公开源码/tag/Release 等发布门禁仍未完成，因此 `release_ready=false`。

## 0.1.0-test（发布候选，未公开）

### 测试版整合包

- 新增 Release 构建、调试符号分离、PE 安全/开发路径审计和确定性公共测试包流程；普通 ZIP 不含调试符号。
- 明确版本为 `TPT-ZH-OmniPack 0.1.0-test`，保留 The Powder Toy `100.0.399` 作为上游基础，不改动协议或官方存档版本逻辑。
- 用可追溯的 The Powder Toy 上游位图字库和 GNU Unifont 16.0.03 所需字符子集重建嵌入字体，并随包提供许可证。
- 测试包拒绝 `powder.pref`、个人存档、图章、账户资料、开发脚本、对象文件、构建缓存及其他非发行内容。
- 当前已实现内容包括 23 个工业冶金元素、10 个高级化学元素、8 个局部生态元素和 7 个受控核工业元素，均可从菜单和图鉴检索。
- 玩家可见模块名称统一为工业冶金、局部生态、高级化学和受控核工业；内部键名和历史来源记录保持不变。
- `ELEMENT_REGISTRY.csv` 为 48 个现有元素直接补齐生产、用途、危险、控制和清理字段，并新增构建门禁。
- 新增官方、四个单模块与四模块混合的真实 OPS stamp 双往返，覆盖稳定 ID、identifier 及 `LAVA/SPRK/MSCR/CONV/VIRS` 的间接引用。
- 建立十个固定压力场景、逐秒帧/粒子与进程 CPU/内存采样、OPS 哈希和机器可读 JSON；首次完整运行发现并修复阻塞式 Lua 的“脚本无响应”。
- 增加压力原始证据判定器与发布报告结构门禁，拒绝损坏 OPS/哈希、缺失字段、非机器值和绕过失败项的 `release_ready=true`。
- 增加线程安全的 Omni 成功事件指标与 Lua 读取/重置接口；压力工具记录总事件、单帧峰值、停止残余事件和七项恢复字段断言。
- 当前候选十个正式场景均完成 60 秒预热 + 600 秒采样并通过完整性能门禁；另有 S09 的 7,200.002 秒有限观测通过独立评估，但 1.0.0 综合两小时长跑仍未执行。
- 最终验收补充脱敏凭据扫描、远端引用检查、最终 ZIP 启动和 Lua 回归证据。PAT 撤销、公开源码匿名克隆和真实 GUI 只读门禁尚未完成，因此该版本仍未公开。

### Phase 7：禁用模块存档加载兼容

- 加载含已关闭模块元素的存档时，显示正常加载、只读加载或取消三个选项。
- 检查直接元素及 `LAVA`、`SPRK`、`MSCR` 等载体持有的元素类型，使用 `TYP()` 保留 OPS 打包元数据。
- 只读加载禁止本地另存、覆盖保存和在线上传，不修改原始存档粒子。

### Phase 6：扩展元素图鉴内容

- 为全部 48 个已实现 OmniPack 元素加入双语配方、生产、用途和危险说明，并接入图鉴搜索。

### Phase 5：受控核工业

- 增加受控反应堆燃料、慢化剂、控制棒、冷却剂、废料、中子发生器和屏蔽体。
- 反应路径限制在 `3x3` 邻域，每帧共用有界事件预算，且不改写官方核元素状态机。

### Phase 4：局部生态

- 增加营养盐、藻类、菌丝、孢子、病原体、消毒剂、腐殖质和生物膜的局部生态循环。

### Phase 3：工业冶金与高级化学

- 增加 23 个工业冶金元素和 10 个高级化学元素，提供有界合金、炼钢、燃料裂化、聚合、发酵和肥料路径。

### Phase 0：来源审计

- 固定九个上游/模组仓库的分支、commit、版本和许可证。
- 记录官方 100.0 元素 ID 与五个旧模组自定义 ID 的冲突。
- 分类可直接保留、必须重写、仅作设计参考和排除的模块。
- 确认 Seppo 公共仓库缺失全部新增元素实现，禁止从二进制复制。
- 确认汉化分支存在默认英文、语言控件回归、`BASE` 硬编码中文、严格 JSON 校验缺失和字体授权不明问题。
- 建立进度、兼容、测试、已知问题、AI 披露和元素稳定 ID 设计文档。

### 尚未完成

- 灾害与特殊物理、内容发现、旧存档迁移、性能/安全加固和正式 RC 冻结。
- 跨来源旧模组存档迁移与兼容占位转换。
- 最终 ZIP 的 GUI 点击、语言切换、本地 `.cps` 保存、只读上传拦截及完整持续压力测试；在这些证据完成前不得公开发布。

### Phase 1：稳定基线

- 完成未经修改的 Dragonrster 100.0.398 Windows x64 静态构建和进程级启动测试。
- 建立 `integration/zh-omnipack` 开发分支。
- 合并官方 100.0.399 及其后续安全修复，人工融合 7 个冲突文件。
- 恢复默认简体中文和 12 语言切换控件。
- 修复 `BASE` 硬编码中文以及官方新增 Intro/FPS 文本的英中本地化。
- 使用独立项目名、可执行文件名、App ID 与数据目录，停止冒用原汉化模组 ID。
- 修复删除陈旧 `VcsTag.h` 后 clean parallel build 的生成依赖竞态。
- 三次静态构建均成功；仓库当前仍没有注册 Meson 测试。

### Phase 2：基础设施

- 固定官方元素 ID 0–195，建立 196 槽锁表和 ID 146 tombstone。
- 建立 0–511 稳定内容分区和严格元素登记检查器。
- 用 JsonCpp 严格解析语言资源，拒绝重复键、无效根类型、尾随垃圾和宽松 JSON 扩展。
- 为 195 个活动官方元素补齐英中正式名称；英中键达到 1,154/1,154，缺失和阻塞错误均为 0。
- 注册 i18n、元素登记和工具单元测试三个 Meson 静态门禁。
- 实现中央模块选择门禁、模块设置页和编译时只读图鉴目录。
- 图鉴搜索支持中英文名称与说明、代号、identifier、来源和分类。
- 图鉴动态枚举键由登记表自动推导并审核，避免新增内容显示裸开发枚举。
- 图鉴生成在每次构建时先执行完整登记语义检查。
- 增加 Lua 动态元素分配/选择运行回归，并验证含空格临时目录。
- Phase 2 Windows x64 clean build 448/448，Meson 静态测试 3/3，工具单元测试 28/28。
