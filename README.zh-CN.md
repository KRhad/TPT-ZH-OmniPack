# TPT-ZH-OmniPack / 万象沙盘整合版

> 本项目是以大量元素、化合物和材料为核心的自由沙盒整合版。项目不包含任务、成就、科技树或强制元素解锁，所有已启用内容均可直接使用。

这是一个非官方、以 The Powder Toy `100.0.399` 为基础、默认简体中文并保留完整英文界面的 GPL-3.0 大型内容整合客户端。项目目标不是任务游戏或生存游戏，而是让玩家打开普通沙盒后直接进行化学、冶金、核工业、生态、材料、电气、高温高压、爆炸和任意创造实验。

周期表统一提供元素单质、同位素和无机化合物，并为现有 93 个有机物、聚合物、合金、矿物、陶瓷、玻璃、半导体、复合及工程材料提供补充关联索引。这 93 条记录仍可从“有机材料”或“合金与工程材料”进入；其中 18 个后来加入的电子材料从官方电子分类迁入“合金与工程材料”，但电子模块行为不变。不同入口始终选择同一个规范 identifier 和稳定 ID。原版 TPT 元素仍保持原菜单位置，OmniPack 自造特殊内容也保留 rc8 原位置。

## 当前真实状态

- 当前引擎登记了 488 个活动槽：官方 195 个，加上 OmniPack 293 个登记项。其中 `OMNI_PT_MSCR=278` 是隐藏兼容别名；去除该别名后共有 487 个活动类型。全部入口合计可选择的真实材料为 466 个（官方 174、OmniPack 292）；其中标准菜单展示 301 个，另有 165 个 OmniPack 周期单质、核素和无机物仅通过周期表详情选择。其余为擦除工具及官方隐藏过渡/辅助类型，不作为材料数量宣传。`total_playable_materials>=300=true`。
- 当前扩展内容：工业冶金/工程材料 43 个可玩材料和 1 个兼容别名、生态/生物/环境 24、受控核工业与代表性核素 20、化学/无机物/两批有机物 93、周期新增元素 92、电子与特殊材料 20；五个可选模块默认启用，周期元素始终直接可用。
- 行为级去重已把旧金属碎料 `MSCR` 合并到官方 `BRMT`：官方金属粉保留原行为，只有携带专用可回收标记的 `BRMT` 才保存来源金属并按其熔点回炉。旧 ID 278 继续读取和迁移，但从菜单、搜索和 Lua 直接创建中移除，且永不复用。
- 当前稳定空间已安全扩展为 `PT_NUM=1024`、`PMAPBITS=10`；旧 `0..669` ID 全部不变。周期表严格复用 26 个现有纯元素实现，其余 92 个固定为 `370..461`；无机三批使用 `462..511`。工程材料区固定为 `512..575`，已实现 `SOLD..RFBK=512..532`，`533..575` 保留；代表性核素固定为 `H2IS..CF52=576..588`；两批有机物固定为 `CH4M..EACT=589..621`；电子与特殊材料首批为 `GAAS..DIEL=622..641`，`642..669` 保留；生态与污染首批为 `SOIL..DETG=670..685`，未来内容从 `686` 开始。OPS 第二类型字节、按来源位宽建立的 identifier palette、旧 8/9 位携带字段、Lua、菜单和两张 `1024²` 移动矩阵均已审计并自动验证；已知 identifier 即使来自更宽 OPS 的 1536 号槽，也会先映射到当前稳定 ID 再做范围检查。
- 已加入独立标准长式周期表面板。元素格单击后打开可滚动材料面板，标题按当前语言显示主名称，下一行弱化显示另一语言名称、原子序数和去重后的关联材料数；面板不再重复“元素说明”正文，只保留“· 长按查看完整说明”。登记共 276 个规范入口：118 个单质、13 个核素、52 个无机物以及 93 个补充材料关系；每个入口都复用原材料记录。玩家可按中文名、英文名、符号、原子序数、化学式或材料名搜索，内部仍兼容 identifier 搜索但不在界面提示中暴露开发术语。面板支持常温状态、放射性、金属/非金属/类金属筛选、类别着色及镧锕系展开。
- 菜单展示策略集中由 `docs/CONTENT_MENU_POLICY.csv` 驱动，不修改元素的物态、反应、稳定 ID 或自身 `MenuVisible`。38 个有机材料进入“有机材料”，55 个合金、矿物、陶瓷、玻璃、半导体、电子和复合工程材料进入“合金与工程材料”；生态、核工业设备与两个 rc8 特殊项保持原位置。
- 所有已启用 OmniPack 内容与完整 118 周期元素均可直接选择、放置，也可由普通 Lua 创建；官方隐藏过渡/辅助类型仍保持上游语义。没有发现进度、配方进度、存档进度或任务完成条件。
- 关闭任一内容模块会阻止对应元素的菜单/Lua 选择与新建；已有 OPS 粒子不删除，但对应冶金、生态环境、化学、核工业或电子材料更新会在下一模拟刻暂停，重新启用后恢复。五模块禁用 OPS 运行回归覆盖 25 个粒子，其中包含 `NITI=520`、`RFBK=532`、`CF52=588`、`EACT=621`、`DIEL=641`、`DETG=685`、不得继续皂化的 `FATS+CAUS`、不得固化的 `2×ERES+CATA`、不得放电的介电陶瓷、不得清洗的 `DETG+OIL+WATR` 和带来源类型的可回收 `BRMT`。
- 旧 OPS 中曾写入的 `omniAlchemy` 字段现在仅作为未知顶层字段被忽略，新存档不再写入它，旧粒子与间接元素字段保持不变。
- 长按图鉴在正文前明确显示“元素说明 / Element description”。全部 195 个官方规范条目均有参考锁定 Wiki 分类页/条目页并由当前源码校正的双语完整说明；22 个液体保留逐项人工说明，其余旧条目使用上游行为摘要并显式修正已知错译、相态错误和截断文本。图鉴还直接读取当前生成温度、热学、重量、重力、耐酸蚀、可燃/爆炸、导电、中子吸收及温压相变目标；292 个 OmniPack 规范材料另有双语配方、生产、用途和危险字段。稳定 ID、源码 commit、实现/测试状态等开发字段不在玩家图鉴中显示。全部 OmniPack 菜单代号统一为唯一四位大写字母；底部周期表按钮与搜索按钮各自保留空间，扩展气体采用与官方气体一致的柔和云团绘制。
- 生态与污染首批已绑定提交 `031c36ff` 完成最终 clean build：`771/771`、Meson `31/31`、Python 202 项（0 fail、2 skip）；同一 EXE 已复跑全部内容模块、完整/简化生态、模块禁用、八类 OPS、mixed OPS、旧别名和旧进度兼容。该开发 EXE 的 SHA-256 为 `154A8D24FA52B54A3F3038C14E7B96728E09B1C6415503D7AB61DE90F634C709`。
- rc9 候选 `f970a534` 的普通/符号 ZIP 审计通过；绑定同一候选的 S15-S20 六项无预热 30 秒稳定性门禁均经独立 assessment 通过，fixture 分别为 `118/118/118`、`50/50/50`、`21/21/21`、`13/13/13`、`33/33/33` 和 `487/484/466`。
- 当前仍是开发状态，`release_ready=false`；`f970a534` 只作为只读参考候选，其哈希和 S15-S20 结论不能转移给后续工作树构建。脏工作树验证包必须在清单中明确记录基线 revision、`source_state`、工作树 SHA-256 和未跟踪文件数，不能冒充干净发布候选。正式 GUI 双语/DPI 视觉矩阵、独立两小时长跑、匿名源码重建、公开源码远端、签名、tag 和 Release 仍未完成。

## 内容路线

正式路线以 [内容扩展路线图](docs/CONTENT_EXPANSION_ROADMAP.md) 为准：

- `0.1.x`：稳定中文、48 个扩展元素、四个现有模块、存档和发布基础；
- `0.2.0`：完整 118 元素周期表；
- `0.3.0`：常见无机化合物、酸、碱、盐和氧化物；
- `0.4.0`：合金、矿物、陶瓷、玻璃和工程材料；
- `0.5.0`：核素、衰变、核燃料和核工业；
- `0.6.0`：有机物、燃料、塑料、溶剂和工业化学；
- `0.7.0`：半导体、电池、超导、磁性和电子材料；
- `0.8.0`：生态、生物材料、污染、毒物和环境反应；
- `0.9.0`：菜单、周期表 UI、搜索、反应、性能和存档整理；
- `1.0.0`：内容冻结、全面测试和正式发布。

周期表 UI 只是选择入口，不是解锁系统。玩家既可以直接放置最终材料，也可以自行模拟矿石加工、化学合成、合金熔炼或核燃料循环。

## 来源与移植原则

外部模组只能作为只读来源。项目不会整库合并旧模组，不会覆盖官方元素 ID，也不会复制无源码、许可证不明或只有二进制的实现。与官方或现有 OmniPack 元素代表同一材料的候选不建立重复 ID；若兼容许可证下存在有价值的行为增量，则作为主元素行为优化重写合并，并回归保护主元素原行为。每个实际移植或增强项必须固定原仓库、commit、文件、许可证、移植类型、主 identifier、稳定 ID、主要改动和测试。

来源记录见：

- `docs/MOD_SOURCE_CATALOG.csv`
- `docs/MOD_LICENSE_AUDIT.md`
- `docs/THIRD_PARTY_SOURCES.md`
- `docs/PORTING_LEDGER.md`

模组素材库已完成搜索、固定、提取和许可证分类。最终发行闭包登记 24 个代码、模组、字体和静态库组件；41 个来源中 20 个授权未知或未验证来源继续保持拒绝/仅参考，22 个实际登记的外部来源元素均来自 GPL-3.0 固定提交。`license_audit_pass=true`；后续仍须对最终 1.0.0 ZIP 实际成员再执行一次许可证映射审计。

## 构建与测试

Windows x64 使用 MSYS2 UCRT64、Meson 和 Ninja。现有构建与测试工具必须保留，它们不是游戏内任务系统。常用命令：

```powershell
$env:PATH='C:\msys64\ucrt64\bin;C:\msys64\usr\bin;' + $env:PATH
meson compile -C build-ui-gas-electronics-final-clean
meson test -C build-ui-gas-electronics-final-clean --suite static --print-errorlogs
C:\msys64\ucrt64\bin\python3.exe -m unittest discover -s tools/tests -p 'test_*.py'
```

Android ARM64 直接移植版使用同一套模拟、元素 ID、中文字体与 OPS 存档代码。移动端构建和签名方法见 [Android 直接移植版](docs/ANDROID_PORT.md)；当前 APK 已完成交叉编译、Manifest、签名、ZIP 和 16 KB ELF 对齐审计，但在连接真实 Android 设备前，触控、输入法、后台恢复和实际帧率仍记为未测试。

测试结果、未测试边界和正式门禁分别见 [TEST_MATRIX](docs/TEST_MATRIX.md)、[KNOWN_ISSUES](docs/KNOWN_ISSUES.md) 和 [VERSION_GATES](docs/VERSION_GATES.md)。自动测试通过不等于 GUI、视觉、长跑或公开发布通过。

## 法律与发布边界

本项目整体依据 GNU GPL v3 发布，并保留 The Powder Toy 及兼容来源作者的版权与通知。发布包必须携带项目 GPL、字体许可证、第三方 manifest 以及 12 份固定静态库许可证；不得包含 `powder.pref`、令牌、个人存档、未登记图章、调试对象或开发者绝对路径。

当前 `origin` 仍指向历史汉化仓库 `Dragonrster/The-Powder-Toy-Chinese`，不是已授权的 OmniPack 正式源码远端，因此不得擅自推送、打 `v1.0.0` tag 或创建公开 Release。只有完整门禁全部为真时，才允许声明正式 `1.0.0`。

> AI 辅助开发披露：本项目在来源审计、代码和文档起草、构建、自动测试与翻译检查中使用 OpenAI Codex / AI 辅助；证据边界见 `docs/AI_DISCLOSURE.md`。
