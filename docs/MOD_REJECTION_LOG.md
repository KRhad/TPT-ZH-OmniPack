# 模组候选拒绝日志

| 来源 | 候选 | 原因 |
|---|---|---|
| `cyens_src@1b74504e` | 旧 `UREA + HNO3 -> UNTR` 高能路径 | 不进入当前尿素实现；本批只保留材料概念，改为有界加压合成和水解，不提供现实危险配比或沿用旧爆炸产物 |
| `cyens_src@1b74504e` | 整库保存、网络、协议、UI、Lua 与模拟核心改动 | 当前 OmniPack 是唯一主干；外部核心只读审计，不合并其保存格式、网络协议或旧元素数组 |

当前拒绝的是两个功能/整库合并方案，不是两个独立元素候选，因此机器统计仍为 `elements_rejected=0`。
