# Windows x64 公共测试候选

## 测试范围

这是 `TPT-ZH-OmniPack 0.1.0-test` 的 Windows x64 公共测试候选。它基于 The Powder Toy `100.0.399`，不是官方发布，也不是稳定版。它包含官方 TPT 100.0
沙盘以及下列已实现模块：

- 工业冶金：ID `256..278`。
- 局部生态：ID `288..295`。
- 受控核工业：ID `328..334`。
- 高级化学：ID `360..369`。

其余预留 ID 区间不代表已完成玩法。自动化、灾害、炼金进度、兼容占位和来源模组
迁移均**不属于本测试版**。

## 最终验收状态

该候选尚未公开发布。公开下载仍受凭据撤销/轮换、公开源码匿名克隆、最终 ZIP 的真实 GUI/OPS/只读门禁和持续压力测试阻塞。完整状态与证据位置见源码仓库 `docs/FINAL_VALIDATION.md`；未完成的实际交互测试不得由本说明中的自动化测试列表替代。

## 启动

1. 将 `TPT-ZH-OmniPack-0.1.0-test-Windows-x64.zip` 解压到可写目录。
2. 用 `TPT-ZH-OmniPack-0.1.0-test-Windows-x64.zip.sha256` 校验 ZIP 哈希；ZIP 内的 `TEST-MANIFEST.txt` 校验每个文件。
3. 启动 `tpt-zh-omnipack.exe`。
4. 在“选项”的内容分区开关各模块，再从材料菜单或元素搜索中查找元素。

测试包刻意不包含偏好、图章、存档、脚本或账户数据；首次运行会自行创建独立于官方 TPT 的应用数据。便携测试包不会请求“安装”程序或注册 `.cps`、`.stm`、`ptsave://` 文件关联；若出现该提示，应视为构建配置回归并停止验收。当前 EXE 未签名，Windows SmartScreen 可能提示；不要绕过来源或哈希核验。

## 手动检查

1. 搜索 `ALUM`、`NUTR`、`NFUL` 和 `CHLR`，确认每项都有中英文图鉴内容。
2. 创建基础流程：熔融铜加锡形成青铜；营养盐、藻类、水和二氧化碳形成生态循环；
   对带燃料和慢化剂的中子发生器通电；以及催化剂辅助的燃料和化学路径。
3. 关闭一个已实现模块，确认该模块工具不可用。
4. 打开包含已关闭模块元素的存档，确认有正常加载、只读加载和取消三项选择；
   只读状态下本地保存及在线上传必须被阻止。
5. 在选项中切换简体中文和英文，确认菜单、内容标签与存档兼容提示均可读。

## 已自动验证

- 使用 GCC 16.1.0 的 Windows x64 clean Release 静态构建，`502/502`。
- Meson 静态测试集 `14/14` 与 Python 工具测试集 `87/87`。
- 模块选择、工业冶金、高级化学、局部生态和受控核工业的 Lua 运行回归。
- 元素登记、双语本地化、图鉴内容与禁用模块存档门禁审计。
- 官方、冶金、生态、化学、核工业及四模块混合 OPS stamp 的保存—退出—重启—加载—再保存—再加载；间接 `ctype/tmp/tmp2` 字段逐项核对。
- 普通 ZIP 与符号 ZIP 白名单、内部清单、SHA-256、解压后二次二进制审计；普通包不含偏好、存档、图章、脚本、账户或调试符号。

## 已知测试限制

- UI 视觉复核与语言切换交互仍需人工验证。
- GUI 本地 `.cps` 保存路径、跨模组迁移、兼容占位、模块事件计数、场景停止/恢复断言与两小时长跑尚未完成。当前候选十个 10 分钟高粒子数样本的执行和独立工件评估已完成，但完整性能门禁仍为 `false`。
- 最终 ZIP 的视觉、语言切换、只读上传和压力事件/场景行为门禁仍需在隔离 Windows x64 环境中实际完成；在报告写明通过前，它阻止公开发布。
