# 纯沙盒内容扩展路线图

## 产品定义

TPT-ZH-OmniPack 是以数百种可操作元素、化合物、合金、核材料和特殊物质为核心的中文 The Powder Toy 大型自由沙盒整合版。所有已启用内容默认直接可用；收藏、最近使用、搜索和周期表只改善选择体验，不参与进度或解锁。

```text
task_driven_game=false
achievement_game=false
technology_tree=false
forced_unlocks=false
all_enabled_content_directly_available=true
target_playable_materials=300..450
```

## 当前基线

| 项目 | 当前值 |
|---|---:|
| 官方活动元素 | 195 |
| OmniPack 登记项 | 191 |
| 兼容别名 | 1 |
| OmniPack 可玩元素 | 190 |
| 引擎活动槽 | 386 |
| 可玩材料合计 | 385 |
| 已登记槽位 | 513 |
| 保留槽位 | 127 |
| 可选内容模块 / 始终可用周期内容 | 4 / 1 |
| 可放置周期元素映射 | 118 / 118 |
| 已登记反应 | 192 |
| `PT_NUM` / `PMAPBITS` | 1024 / 10 |
| 存档 | OPS1 / BZip2 / palette identifier |

现有稳定 ID 不重编号。容量审计已把引擎扩展到 1024 槽：`196..255` 只供 Lua 首选动态分配，92 个缺失周期元素固定为 `370..461`，无机三批固定为 `462..511`，工程材料固定使用 `512..575`，未来区为 `576..1023`。`MSCR=278` 已合并到官方 `BRMT=30`，但 278 仍是永久兼容墓碑，不能重新分配。OPS 第二类型字节、identifier palette、旧 8/9 位 `ctype/tmp/tmp2` 重打包、损坏 `pmapbits` 拒绝、Lua 高位分配、模块和两张 `1024²` 移动矩阵均已有门禁。

## Phase 1：方向清理

- 删除玩家炼金进度、选择锁、进度窗口、Lua 进度 API、OPS 写入和 0.4 打包配置；
- 删除任务、成就、挑战、科技树、发现和强制解锁的未来计划；
- 保留并重命名开发用反应样例、回归场景、构建脚本、压力测试和发布门禁；
- 旧 `omniAlchemy` OPS 字段忽略读取，新存档不再写入；
- 删除没有实际内容的玩家设置入口，保留稳定 ID 分区；
- 图鉴正文增加明确的“元素说明 / Element description”标签。

## Phase 2：周期表基础设施

1. 已审计官方、OmniPack 和合法模组候选对真实纯元素的代表性：复用 26 个，新增 92 个；
2. 已建立 `PERIODIC_ELEMENT_SOURCE_MAP.csv` 与原子序数到稳定 ID 的不可变映射；
3. 已建立 118 行编译时元数据、元素族共享逻辑和数据驱动反应登记；
4. 已建立标准长式周期表 UI、搜索、筛选、双语名称、类别着色和 f 区展开；
5. 周期表 UI 不改变菜单可用性，也不读取进度；编译与静态契约通过，完整桌面视觉矩阵仍独立为 `NOT RUN`。

## Phase 3：118 元素

按族分批实现并逐批登记、构建、测试和提交：

1. **已完成首批**：氢复用及氦、氖、氩、氪、氙、氡、鿫；
2. **已完成第二批**：复用锂、铷并新增钠、钾、铯、钫；
3. **已完成第三批**：复用镁并新增铍、钙、锶、钡、镭；
4. **已完成第四批**：复用铝并新增硼、镓、铟、铊、鿭；
5. **已完成第五批**：复用钻石、硅、锡、铅并新增锗、𫓧；
6. **已完成第六批**：新增氮、磷、砷、锑、铋、镆，并连接官方液氮/氮冰相变；
7. **已完成第七批**：复用官方氧、液氧与钋并新增硫、硒、碲、𫟷；
8. **已完成第八批**：复用既有氯并新增氟、溴、碘、砹、鿬；
9. **已完成第九批**：复用钛、铬、铁、钴、镍、铜、锌并新增钪、钒、锰，完成第一过渡系；
10. **已完成第十批**：复用钼并新增钇、锆、铌、锝、钌、铑、钯、银、镉，完成第二过渡系；
11. **已完成第十一批**：复用钨、铂、金、汞并新增铪、钽、铼、锇、铱，完成第三过渡系；
12. **已完成第十二批**：新增镧至镥 15 个成员，以共享有界逻辑区分储氢、储氧、磁性、荧光、光子放大、中子吸收、钷衰变和镱温水反应；
13. **已完成第十三批**：复用官方铀、钚并新增锕、钍、镤、镎、镅、锔、锫、锎、锿、镄、钔、锘、铹；共享逻辑实现压缩寿命、代表性衰变、中子合成链、锎有限裂变代理、酸氧反应和熔融行为；
14. **已完成第十四批**：104–112 号九个超重过渡元素全部新增，并与既有 113–118 号组成 15 成员超重系列；使用差异物性、有限寿命、代表性后代和逐级高能中子合成，不产生永久稳定空壳。

同族可共享底层逻辑，但必须有实际属性或用途差异。超重元素必须有有界寿命和事件预算，不能表现为永久稳定铁块。

## Phase 4–9：内容扩展

无机化学已完成三批共 50 种精确材料并复用官方 `SALT` 作为氯化钠映射。工程材料批次已经以 `SOLD=512` 启动：三份熔融锡与两份熔融铅形成低熔点锡铅合金，支持通电熔断、压力碎料和按来源回炉。其余数量目标仍是内容审计方向，不以纯换色或无反应差异材料凑数。

| Phase | 内容 | 目标范围 |
|---|---|---:|
| 4 | 酸、碱、盐、氧化物和工业无机物 | 50–100 |
| 5 | 合金、矿物、陶瓷、玻璃和建筑材料 | 45–90 |
| 6 | 代表性核素、核燃料、屏蔽与废料 | 15–30 |
| 7 | 有机物、燃料、溶剂、聚合物和高能材料 | 25–50 |
| 8 | 半导体、磁性、超导、复合与纳米材料 | 20–40 |
| 9 | 生态、生物材料、污染和毒物 | 20–40 |

最终材料可直接放置；生产链是可选沙盒玩法，不是获得材料的条件。

## Phase 10：内容整理

- 纯换色、行为等价且没有增量的候选不收录；代表同一材料但包含有价值行为增量时，把增量作为官方或现有主元素增强合并，不建立第二个菜单元素或用兼容墓碑凑数量；
- 完成中文名、英文名、符号、identifier、原子序数和分类搜索；
- 完成图鉴来源、属性、用途、危险和“游戏化而非科研级精确模拟”说明；
- 完成统一事件预算、模块关闭、OPS 往返、缺失元素和大型混合存档测试；
- 确认 300+ 材料仍在性能与 ID 容量内。

## Phase 11：1.0.0 发布

内容冻结后执行 clean Release build、全量自动测试、两小时长跑、GUI 双语/DPI/字体人工矩阵、包审计、PE 安全标志、许可证、对应公开源码、匿名克隆重建、`v1.0.0` tag 和正式 Release。

外部 PAT、正式远端授权和人工 GUI 是真实外部边界；边界存在时仍继续完成全部本地内容，但 `release_ready` 必须保持 `false`。
